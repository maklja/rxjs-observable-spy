{
	"name": "@maklja90/observable-spy",
	"private": true,
	"workspaces": [
		"packages/*"
	],
	"devDependencies": {
		"@types/mocha": "^9.0.0",
		"@typescript-eslint/eslint-plugin": "^5.23.0",
		"@typescript-eslint/parser": "^5.23.0",
		"eslint": "^8.15.0",
		"eslint-config-prettier": "^8.5.0",
		"eslint-plugin-chai-expect": "^3.0.0",
		"eslint-plugin-prettier": "^4.0.0",
		"lerna": "^5.4.2",
		"mocha": "^9.0.3",
		"prettier": "^2.7.1",
		"rimraf": "^3.0.2"
	},
	"scripts": {
		"bootstrap": "lerna bootstrap",
		"build": "lerna run build",
		"clean": "lerna run clean",
		"test": "lerna run test",
		"format": "lerna run format",
		"lint": "lerna run lint",
		"version": "lerna version --conventional-commits --changelog-preset angular --no-private --yes",
		"publish": "lerna publish from-git --ignore-scripts --no-private --yes"
	},
	"author": "Bojan Makljenovic",
	"license": "MIT",
	"repository": {
		"type": "git",
		"url": "https://github.com/maklja/rxjs-observable-spy"
	}
}

